const WIN_HEIGHT = 600, WIN_WIDTH = 800;

const SPRITE_KEYS =
  {
    skeleton: 'skeleton',
    house: 'house',
    tiles: 'tiles',
    zombie: 'zombie'
  };

const MAP_KEY = 'map';

export
{
  WIN_HEIGHT, WIN_WIDTH,

  SPRITE_KEYS, MAP_KEY
}
